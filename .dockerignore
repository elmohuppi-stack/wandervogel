# Was nicht ins Bauverzeichnis gehört.
#
# `data/` ist der größte Posten und fliegt vollständig raus: dort liegen die
# BRouter-Segmente (720 MB), das Postgres-Datenverzeichnis und die Prüfbilder.
# Betriebsskripte liegen dagegen in `scripts/` und kommen mit — sie gehören
# ins Bild und, anders als data/, auch ins Repository.
data/

.git
.github
node_modules
build
.svelte-kit
.run
.env
.env.*
!.env.example
docs
*.md
!README.md
docker-compose.dev.yml
e2e-*.png
